name: clepy_caches
description: cache app clepy
version: 0.0.1

publish_to: 'Clepy team'

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_rating_bar: ^4.0.0
  fancy_drawer: ^2.0.0
  dot_navigation_bar: ^1.0.1+4
  shared_preferences: ^2.0.15

  domain:
    path: ../domain


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
