name: shared
description: Shared Classes
version: 0.0.1

publish_to: none

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  flutter_bloc: ^8.0.1
  shared_preferences: ^2.0.15
  flutter_svg: ^1.1.0
  
  clepy_caches:
    path: ../clepy_caches
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0